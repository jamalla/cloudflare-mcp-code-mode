# cloudflare-mcp-code-mode

Minimal demo of a Cloudflare `MCP` weather server, `agent` worker, and `frontend` chat.
Includes an early `Code Mode` integration path for agent tooling.

Run locally: `pnpm install && pnpm dev`